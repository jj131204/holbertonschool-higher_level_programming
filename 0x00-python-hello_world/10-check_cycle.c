#include "lists.h"

/**
 *check_cycle - prototype
 *
 *@list: pointer to cycle.
 *
 *Return: 0 or 1
 */

int check_cycle(listint_t *list)
{
	if (!list)
		return (0);

	else
		return (1);

return (0);
}
